export const comboboxConfig = {
  maxDropdownHeight: '300px',
  triggerHeight: '40px',
  inputBackground: 'bg-background',
  hoverBackground: 'bg-accent',
  hoverText: 'text-accent-foreground',
  selectedBackground: 'bg-accent',
  selectedText: 'text-accent-foreground',
  width: {
    min: '200px',
    max: '100%'
  }
}

export const sidebarConfig = {
  defaultWidth: 250,
  minWidth: 200,
  maxWidth: 600,
  resizeTransition: 'width 150ms ease-in-out',
  padding: {
    x: 4, // 16px in Tailwind
    y: 4
  }
}